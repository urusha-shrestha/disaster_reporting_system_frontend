/*import 'package:flutter/material.dart';

double xOffset = 0;
double yOffset = 0;
double scaleFactor = 1;
bool isDrawerOpen = false;

class drawer_closed extends StatefulWidget {

  @override
  _drawer_closedState createState() => _drawer_closedState();
}

class _drawer_closedState extends State<drawer_closed> {
  @override
  Widget build(BuildContext context) {
    return IconButton(
        icon: Icon(Icons.menu),
        onPressed: () {
          setState(() {
            xOffset = 230;
            yOffset = 100;
            scaleFactor = 0.8;
            isDrawerOpen = true;
          });
        });
  }
}


class drawer_open extends StatefulWidget {

  @override
  _drawer_openState createState() => _drawer_openState();
}

class _drawer_openState extends State<drawer_open> {
  @override
  Widget build(BuildContext context) {
    return IconButton(
        icon: Icon(Icons.arrow_back_ios),
        onPressed: () {
          setState(() {
            xOffset = 0;
            yOffset = 0;
            scaleFactor = 1;
            isDrawerOpen = false;
          });
        });
  }
}*/
